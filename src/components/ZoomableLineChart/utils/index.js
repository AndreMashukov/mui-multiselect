import { updateDots } from "./updateDots";
import { createSvg } from "./createSvg";
import { createAxes } from "./createAxes";
import { addClipping } from "./addClipping";

const utils = {
  createSvg,
  updateDots,
  createAxes,
  addClipping,
};

export default utils;
